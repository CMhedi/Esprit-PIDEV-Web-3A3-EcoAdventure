{% extends 'admin/base_admin.html.twig' %}

{% block title %}Nouvel Événement{% endblock %}
{% block page_title %}Nouvel Événement{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h2 class="fw-bold mb-0">✨ Créer un Nouvel Événement</h2>
                    <p class="text-muted">Remplissez les informations ci-dessous pour publier une nouvelle aventure.</p>
                </div>
                <a href="{{ path('app_event_admin_index') }}" class="btn btn-outline-secondary"><i class="bi bi-arrow-left"></i> Retour à la liste</a>
            </div>

            <div class="card border-0 shadow-sm rounded-4">
                <div class="card-body p-4 p-md-5">
                    {{ include('admin/event/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
