# config/services.yaml

parameters:
    messages_upload_directory: 'C:/xampp2002/htdocs/uploads'
    # dossier pour uploader les images des activites
    upload_dir: 'C:/wamp64/www/uploads'

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'